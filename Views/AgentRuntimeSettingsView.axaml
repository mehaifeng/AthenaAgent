<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Athena.UI.ViewModels"
             xmlns:loc="using:Athena.UI.Markup"
             x:Class="Athena.UI.Views.AgentRuntimeSettingsView"
             x:DataType="vm:AgentRuntimeSettingsViewModel">
    <ScrollViewer>
        <StackPanel Margin="28" MaxWidth="960" HorizontalAlignment="Stretch" Spacing="18">
            <StackPanel Spacing="4">
                <TextBlock Text="{loc:Loc Settings.Agent.Title}"
                           FontSize="{DynamicResource App.FontSize.PageTitle}"
                           FontWeight="SemiBold"/>
                <TextBlock Text="{loc:Loc Settings.Agent.Description}"
                           TextWrapping="Wrap"
                           Opacity="0.62"/>
            </StackPanel>

            <Border Padding="18"
                    Background="{DynamicResource App.SectionBg}"
                    BorderThickness="1"
                    BorderBrush="{DynamicResource SemiGreyLowBrush}"
                    CornerRadius="10">
                <StackPanel Spacing="16">
                    <TextBlock Text="{loc:Loc Settings.Agent.Conversation.Title}"
                               FontSize="{DynamicResource App.FontSize.Title}"
                               FontWeight="SemiBold"/>
                    <Grid ColumnDefinitions="300,180,*">
                        <TextBlock Text="{loc:Loc Settings.Agent.Conversation.MaxParallel}" VerticalAlignment="Center"/>
                        <NumericUpDown Grid.Column="1"
                                       Minimum="1"
                                       Maximum="16"
                                       Value="{Binding State.Config.MainConversationMaxParallel}"/>
                    </Grid>
                    <Grid ColumnDefinitions="300,180,*">
                        <TextBlock Text="{loc:Loc Settings.Agent.Conversation.MaxToolRounds}" VerticalAlignment="Center"/>
                        <NumericUpDown Grid.Column="1"
                                       Minimum="1"
                                       Maximum="200"
                                       Value="{Binding State.Config.MainConversationMaxIterations}"/>
                    </Grid>
                    <TextBlock Text="{loc:Loc Settings.Agent.Conversation.MaxToolRoundsHint}"
                               TextWrapping="Wrap"
                               Opacity="0.58"/>
                </StackPanel>
            </Border>

            <Border Padding="18"
                    Background="{DynamicResource App.SectionBg}"
                    BorderThickness="1"
                    BorderBrush="{DynamicResource SemiGreyLowBrush}"
                    CornerRadius="10">
                <StackPanel Spacing="16">
                    <TextBlock Text="{loc:Loc Settings.Agent.SubAgent.Title}"
                               FontSize="{DynamicResource App.FontSize.Title}"
                               FontWeight="SemiBold"/>
                    <CheckBox Content="{loc:Loc Settings.Agent.SubAgent.Enabled}"
                              IsChecked="{Binding State.Config.EnableSubAgents}"/>
                    <TextBlock Text="{loc:Loc Settings.Agent.SubAgent.ModelHint}"
                               TextWrapping="Wrap"
                               Opacity="0.58"/>
                    <Grid ColumnDefinitions="300,180,*">
                        <TextBlock Text="{loc:Loc Settings.Agent.SubAgent.MaxParallel}" VerticalAlignment="Center"/>
                        <NumericUpDown Grid.Column="1"
                                       Minimum="1"
                                       Maximum="16"
                                       Value="{Binding State.Config.SubAgentMaxParallel}"/>
                    </Grid>
                    <Grid ColumnDefinitions="300,180,*">
                        <TextBlock Text="{loc:Loc Settings.Agent.SubAgent.MaxToolRounds}" VerticalAlignment="Center"/>
                        <NumericUpDown Grid.Column="1"
                                       Minimum="1"
                                       Maximum="100"
                                       Value="{Binding State.Config.SubAgentMaxIterations}"/>
                    </Grid>
                    <Grid ColumnDefinitions="300,180,*">
                        <TextBlock Text="{loc:Loc Settings.Agent.SubAgent.Timeout}" VerticalAlignment="Center"/>
                        <NumericUpDown Grid.Column="1"
                                       Minimum="30"
                                       Maximum="3600"
                                       Value="{Binding State.Config.SubAgentTimeoutSeconds}"/>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
